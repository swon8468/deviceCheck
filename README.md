# 디바이스 체커 - 학교 상벌점 관리 시스템

학교에서 학생들의 디바이스 사용과 관련된 상벌점을 체계적으로 관리할 수 있는 웹 애플리케이션입니다.

## 🎯 주요 기능

### 1. 계정 관리
- **최고 관리자**: 클래스, 교사, 학생 계정 생성/수정/삭제
- **담임 교사**: 담당 클래스 학생 관리 및 상벌점 직접 추가
- **교과목 교사**: 상벌점 요청 전송
- **학생**: 자신의 상벌점 조회

### 2. 상벌점 시스템
- 상점/벌점 구분 관리
- 사유별 분류 (휴대폰 사용, 태블릿 사용, 기타)
- 점수 체계 (1회, 2회 등)
- 변경 내역 추적
- 승인/거부 프로세스
- **실시간 점수 업데이트**: 상벌점 등록 시 즉시 학생 누계 점수 반영

### 3. 클래스 관리
- 학년/반별 클래스 구성
- 담임 교사 및 교과목 교사 배정
- 학생 할당 및 관리
- **학년 관리**: 동일 학년 다른 반 학생 목록 조회 및 관리

### 4. 요청 관리 시스템
- **교과목 교사 요청**: 상벌점 요청 전송 및 상태 추적
- **담임 교사 승인**: 요청 검토 및 승인/거부 처리
- **요청 내역**: 모든 요청의 상세 정보 및 처리 상태 관리

### 5. 데이터 관리
- **통합 데이터베이스**: `merit_demerit_records` 컬렉션으로 모든 상벌점 기록 통합 관리
- **실시간 동기화**: Firebase Firestore를 통한 실시간 데이터 동기화
- **데이터 일관성**: 모든 사용자 간 데이터 일관성 보장

## 🏗️ 기술 스택

- **Frontend**: React + Vite
- **UI Framework**: Material-UI (MUI)
- **Backend**: Firebase
  - **Authentication**: 교사/관리자 계정 관리
  - **Firestore**: 데이터베이스
- **상태 관리**: React Context API
- **스타일링**: Emotion (MUI 기본)

## 🔐 인증 시스템

### 학생 계정
- **로그인 방식**: 이름 + 생년월일(6자리) + 학번(5자리)
- **관리**: Firestore 문서 ID 기반
- **보안**: 간단한 정보 매칭으로 인증

### 교사/관리자 계정
- **로그인 방식**: 이메일 + 비밀번호
- **관리**: Firebase Authentication
- **보안**: 표준 이메일/비밀번호 인증
- **계정 생성**: 최고 관리자만 가능

## 📱 반응형 디자인

- 모바일, 태블릿, PC 모든 디바이스 지원
- Material-UI 기반 현대적 UI/UX
- 중앙 정렬 기본 원칙 적용
- **모바일 최적화**: 각 역할별 모바일 UI 개선
- **반응형 테이블**: 화면 크기에 따른 테이블 레이아웃 자동 조정
- **드로어 네비게이션**: 모바일에서 사이드바 형태의 네비게이션

## 🔄 최근 개선사항 (v2.0)

### 🐛 버그 수정
- **벌점 요청 오류 수정**: 교과목 교사가 벌점 요청 시 음수로 정확하게 저장
- **요청자 정보 표시**: 담임교사 승인 대기 목록에서 요청자 정보 정확하게 표시
- **점수 계산 오류**: `cumulativeScore` 업데이트 시 이중 계산 방지
- **중복 기록 제거**: 학생 상세 정보에서 중복된 상벌점 기록 제거
- **상세 사유 처리**: 빈칸일 때 사유로 덮어쓰지 않도록 개선

### 🎨 UI/UX 개선
- **점수 시각화**: 칩 형태로 점수 표시 (양수: 초록색, 음수: 빨간색)
- **날짜 형식**: 상세한 날짜/시간 정보 표시 (년-월-일 시:분:초)
- **요청 내역 테이블**: 시각적으로 개선된 요청 목록 표시
- **모바일 UI**: 각 역할별 모바일 인터페이스 최적화

### 🔧 시스템 개선
- **데이터베스 통합**: `student_merit_logs`를 `merit_demerit_records`로 통합
- **실시간 동기화**: 모든 사용자 간 실시간 데이터 동기화
- **처리 교사 정보**: 정확한 처리 교사 정보 표시
- **검색 기능**: 학년 관리 탭에 학생 검색 기능 추가

### 📊 정렬 기능 추가
- **학년 관리**: 학번, 이름, 담임교사별 정렬
- **요청 내역**: 학생, 요청자, 요청내용, 요청일, 액션별 정렬
- **학생 관리**: 이름, 학번, 상벌점별 정렬
- **상벌점 기록**: 학생, 사유, 요청자, 승인일별 정렬

### 🆕 최신 기능 (v2.1)
- **최고관리자 학생 관리 개선**: 
  - 누계 점수 컬럼 추가 (생년월일과 작업 사이)
  - 학번/이름 클릭 시 상벌점 이력 조회
  - 누계 점수로 정렬 가능
  - 상벌점 이력 다이얼로그 구현
- **전체 시스템 로그 강화**:
  - 상세한 로그 카테고리 추가 (보안, 감사, 성능)
  - 모든 주요 기능에 대한 상세 로그 추적
  - 오류 및 성능 모니터링
  - 보안 이벤트 추적

## 🌐 라이브 데모

**메인 도메인**: [https://checker.bestsite.kr](https://checker.bestsite.kr)  
**GitHub Pages**: [https://swon8468.github.io/deviceCheck](https://swon8468.github.io/deviceCheck)

## 🚀 설치 및 실행

### 1. 프로젝트 클론
```bash
git clone https://github.com/swon8468/deviceCheck.git
cd deviceCheck
```

### 2. 의존성 설치
```bash
npm install
```

### 3. 환경변수 설정
```bash
# .env 파일 생성
cp .env.example .env

# .env 파일을 열어서 Firebase 설정 정보 입력
# Firebase Console에서 프로젝트 설정 > 일반 탭에서 확인 가능
```

**필수 환경변수:**
- `VITE_FIREBASE_API_KEY`: Firebase API 키
- `VITE_FIREBASE_AUTH_DOMAIN`: Firebase 인증 도메인
- `VITE_FIREBASE_PROJECT_ID`: Firebase 프로젝트 ID
- `VITE_FIREBASE_STORAGE_BUCKET`: Firebase 스토리지 버킷
- `VITE_FIREBASE_MESSAGING_SENDER_ID`: Firebase 메시징 발신자 ID
- `VITE_FIREBASE_APP_ID`: Firebase 앱 ID
- `VITE_FIREBASE_MEASUREMENT_ID`: Firebase 측정 ID

### 4. 개발 서버 실행
```bash
npm run dev
```

### 4. 프로덕션 빌드 및 배포
```bash
# 프로덕션 빌드
npm run build

# GitHub Pages 배포
npm run deploy
```

## 🔧 Firebase 설정

### 1. Authentication 활성화
- 이메일/비밀번호 로그인 방식 활성화
- 교사/관리자 계정은 이 방식으로 관리

### 2. Firestore 데이터베이스 생성
- 보안 규칙 설정 (`firestore.rules` 참조)
- 컬렉션 구조는 코드 내 주석 참조

### 3. 보안 규칙
- 학생 계정: 문서 ID 기반 접근
- 교사/관리자: Firebase Auth UID 기반 접근
- 역할별 권한 제한

## 📊 데이터베이스 구조

### 컬렉션
1. **accounts**: 사용자 계정 정보
   - `cumulativeScore`: 학생의 누계 상벌점 점수
   - `role`: 사용자 역할 (super_admin, homeroom_teacher, subject_teacher, student)
   - `uid`: Firebase Auth UID (교사/관리자용)
   - `studentId`: 학생 학번 (5자리)

2. **classes**: 클래스 정보
   - `grade`: 학년
   - `classNumber`: 반 번호
   - `homeroomTeacher`: 담임교사 정보

3. **merit_demerit_records**: 상벌점 기록 (통합 컬렉션)
   - `studentId`: 학생 학번
   - `type`: 상벌점 구분 (merit/demerit)
   - `points`: 점수 (양수: 상점, 음수: 벌점)
   - `reason`: 사유
   - `description`: 상세 설명
   - `processedTeacherName`: 처리 교사 이름
   - `createdAt`: 등록일시

4. **merit_demerit_requests**: 상벌점 요청
   - `studentId`: 학생 학번
   - `requestingTeacherName`: 요청 교사 이름
   - `type`: 상벌점 구분
   - `points`: 점수
   - `reason`: 사유
   - `description`: 상세 설명
   - `status`: 상태 (pending, approved, rejected)
   - `createdAt`: 요청일시

5. **merit_reasons**: 상벌점 사유
   - `type`: 구분 (merit/demerit)
   - `reason`: 사유 내용

### 주요 필드
- `role`: 사용자 역할 (super_admin, homeroom_teacher, subject_teacher, student)
- `uid`: Firebase Auth UID (교사/관리자용)
- `studentId`: 학생 학번 (5자리)
- `type`: 상벌점 구분 (merit/demerit)
- `points`: 점수 (양수: 상점, 음수: 벌점)

## 🎨 UI/UX 특징

- **Material Design**: Google의 Material Design 가이드라인 준수
- **직관적 인터페이스**: 사용자 친화적 디자인
- **탭 기반 네비게이션**: 역할별 기능 분리
- **반응형 테이블**: 데이터 표시 최적화
- **다이얼로그**: 모달 기반 입력 폼

## 🔒 보안 고려사항

- Firebase Authentication으로 교사/관리자 계정 보호
- Firestore 보안 규칙으로 데이터 접근 제한
- 역할 기반 권한 관리
- 학생 계정은 간단한 정보 매칭으로 인증

## 📝 사용법

### 학생
1. 이름, 생년월일, 학번으로 로그인
2. 자신의 상벌점 요약 확인
3. 상세 내역 테이블로 조회

### 교사
1. 이메일/비밀번호로 로그인
2. 역할에 따른 대시보드 접근
3. 상벌점 관리 및 요청 처리

### 관리자
1. 이메일/비밀번호로 로그인
2. 시스템 전체 관리
3. 계정 생성 및 권한 관리

## 🤝 기여 방법

1. Fork the Project
2. Create your Feature Branch (`git checkout -b feature/AmazingFeature`)
3. Commit your Changes (`git commit -m 'Add some AmazingFeature'`)
4. Push to the Branch (`git push origin feature/AmazingFeature`)
5. Open a Pull Request

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다. 자세한 내용은 `LICENSE` 파일을 참조하세요.

## 📞 문의

프로젝트에 대한 문의사항이 있으시면 이슈를 생성해 주세요.
